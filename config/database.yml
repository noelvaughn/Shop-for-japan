# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: mysql
  database: shop_for_japan_dev
  user: root
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: mysql
  database: shop_for_japan_test
  user: root
  password:

production:
  adapter: mysql
  database: shop_for_japan_prod
  user: root
  password:

cucumber:
  <<: *test